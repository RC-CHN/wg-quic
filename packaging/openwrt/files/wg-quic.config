# Each enabled instance is supervised by procd at boot. The section name is
# used as the interface name unless option name is set explicitly.
#
# config instance 'aws'
#	option enabled '1'
#	option config '/etc/wg-quic/aws.conf'
